input {
	tcp {
        type => "json"
		port => 5000
	}
}

filter {
  json {
    source => "message"
  }
}

output {
    if ("" in [message]) {
        elasticsearch {
            hosts => "elasticsearch:9200"
            index => "rest"            
        }
    }
    else {
        elasticsearch {
            hosts => "elasticsearch:9200"
        }
    }
}
